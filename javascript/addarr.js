let colors=["red","green","blue"]
console.log(colors);

colors.push("yellow"); // Add "yellow "to the end of the array
console.log(colors);

colors.unshift("purple"); // Add "purple"to the beginning of the array
console.log(colors);

colors.shift(); //Remove the first element of the array
console.log(colors);

colors[0]="orange"; //change the first element of the array
console.log(colors);

let Girls = ["Cecilie", "Lone"];
let Boys = ["Emil", "Tobias", "Linus"];
console.log(Girls);
console.log(Boys);
let children=Girls.concat(Boys); //combine two arrays(concatenation of arrays)
console.log(children);






